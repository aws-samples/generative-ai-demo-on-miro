{
  "name": "miro-app-wat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy": "(cd frontend && npm install && npm run build) && (cd infrastructure && npm install)",
    "deploy": "(cd infrastructure && npm run deploy)",
    "destroy" : "(cd infrastructure && cdk destroy)"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^18.11.18",
    "esbuild": "^0.16.13",
    "prettier": "2.8.1"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.241.0",
    "jsonwebtoken": "^9.0.0"
  }
}
